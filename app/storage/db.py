def get_database():
    raise RuntimeError("MongoDB temporarily disabled during local smoke testing.")


def get_jobs_collection():
    raise RuntimeError("MongoDB temporarily disabled during local smoke testing.")
